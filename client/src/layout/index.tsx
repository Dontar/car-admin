import { AppBar, AppBarProps, Layout, LayoutProps } from "ra-ui-materialui";

export const MyAppBar = (props: AppBarProps) => (
    <AppBar {...props} />
);

export const MyLayout = (props: LayoutProps) => (
    <Layout {...props} />
);
